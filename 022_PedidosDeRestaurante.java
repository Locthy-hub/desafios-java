/*
 * ==========================================================
 * DESAFIO 022 - Sistema de Pedidos de Restaurante
 * ==========================================================
 *
 * OBJETIVO:
 * Combinar arrays, switch, for e métodos para montar e fechar a conta de um pedido de
 * restaurante.
 *
 * ENUNCIADO:
 * Crie um sistema de pedidos para um restaurante com um cardápio fixo de 5 itens (nome e
 * preço definidos no código). O cliente escolhe itens do cardápio pelo número repetidamente
 * até optar por fechar a conta, informando também a quantidade de cada item escolhido.
 *
 * REGRAS:
 * - Utilize arrays para armazenar o cardápio (nomes e preços) e um array para acumular a
 * - quantidade pedida de cada item.
 * - Utilize switch (ou o próprio índice) para localizar o item escolhido no cardápio.
 * - Ao fechar a conta, exiba todos os itens pedidos (com quantidade > 0), o subtotal de cada
 * - um e o total geral.
 * - Adicione uma taxa de serviço de 10% sobre o total, exibida separadamente antes do total
 * - final.
 *
 * ENTRADA:
 * Escolhas de itens do cardápio (número do item e quantidade) até a opção de fechar a conta.
 *
 * SAIDA ESPERADA:
 * O cardápio ao início, os itens pedidos com subtotal, a taxa de serviço e o total final da conta.
 *
 * EXEMPLOS:
 * Exemplo 1:
 * Entrada:
 *   1
 *   2
 *   0
 * Saida:
 *   Item adicionado ao pedido!
 *   
 *   --- Conta ---
 *   Item 1 - Qtd: 2 - Subtotal: R$ 40.0
 *   Total sem taxa: R$ 40.0
 *   Taxa de servico (10%): R$ 4.0
 *   Total final: R$ 44.0
 *
 * CASOS DE TESTE PARA VALIDAR SUA SOLUCAO:
 * Caso 1: Cliente escolhe o mesmo item mais de uma vez (deve somar a quantidade, não
 * sobrescrever).
 * Caso 2: Cliente fecha a conta sem escolher nenhum item.
 * Caso 3: Cliente escolhe um número de item inválido (fora de 1 a 5).
 *
 * OBSERVACOES IMPORTANTES:
 * Defina o cardápio com nomes e preços fixos no início do código (por exemplo, um array de Strings e um array de double).
 *
 * ==========================================================
 */

public class Main {

    public static void main(String[] args) {

        // Escreva sua solucao aqui

    }

}
