/*
 * ==========================================================
 * DESAFIO 019 - Calculadora de Média Ponderada com Validação de Pesos
 * ==========================================================
 *
 * OBJETIVO:
 * Combinar arrays, métodos, if/else e operadores para calcular médias ponderadas com
 * validação de entrada.
 *
 * ENUNCIADO:
 * Crie uma calculadora de média ponderada genérica. O usuário informa quantas notas serão
 * utilizadas e, para cada uma, a nota e o peso correspondente. O programa deve validar se a
 * soma dos pesos é igual a 10 antes de calcular a média; caso não seja, deve rejeitar o
 * cálculo e informar o motivo.
 *
 * REGRAS:
 * - Notas devem estar entre 0 e 10; pesos devem ser números positivos.
 * - Crie um método somarPesos(double[] pesos) e um método calcularMediaPonderada(double[]
 * - notas, double[] pesos).
 * - Se a soma dos pesos for diferente de 10, exiba uma mensagem de erro e não calcule a média.
 * - Se todas as validações passarem, exiba a média ponderada final formatada com duas casas
 * - decimais.
 *
 * ENTRADA:
 * Quantidade de notas e, para cada uma, o valor da nota e o peso correspondente.
 *
 * SAIDA ESPERADA:
 * Mensagem de erro (se a soma dos pesos for inválida) ou a média ponderada final.
 *
 * EXEMPLOS:
 * Exemplo 1:
 * Entrada:
 *   3
 *   8
 *   3
 *   7
 *   3
 *   9
 *   4
 * Saida:
 *   Media ponderada final: 8.10
 *
 * CASOS DE TESTE PARA VALIDAR SUA SOLUCAO:
 * Caso 1: Soma dos pesos diferente de 10 (deve gerar erro).
 * Caso 2: Nota fora do intervalo 0-10 (deve gerar erro de validação).
 * Caso 3: Apenas uma nota com peso 10 (caso extremo válido).
 *
 * OBSERVACOES IMPORTANTES:
 * Realize todas as validações antes de efetuar o cálculo, evitando processar dados inconsistentes.
 *
 * ==========================================================
 */

public class Main {

    public static void main(String[] args) {

        // Escreva sua solucao aqui

    }

}
