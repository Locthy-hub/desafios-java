/*
 * ==========================================================
 * DESAFIO 016 - Tabela de Classificação de Torneio de Futebol
 * ==========================================================
 *
 * OBJETIVO:
 * Praticar arrays paralelos, for, métodos e operadores aritméticos para calcular uma tabela
 * de pontos corridos.
 *
 * ENUNCIADO:
 * Simule a apuração de resultados de um torneio com 4 times. Para cada rodada de jogos
 * informada, atualize vitórias, empates, derrotas e pontos de cada time (vitória = 3 pontos,
 * empate = 1 ponto, derrota = 0 pontos), e ao final exiba a tabela de classificação ordenada
 * por pontos.
 *
 * REGRAS:
 * - Utilize arrays paralelos: nomes dos times, vitórias, empates, derrotas e pontos.
 * - Para cada partida informada (dois índices de time e o placar de cada um), atualize as
 * - estatísticas dos dois times envolvidos.
 * - Crie um método atualizarEstatisticas(...) que recebe os arrays, os índices dos times e o
 * - resultado da partida.
 * - Ao final, ordene os times por pontos em ordem decrescente (pode usar um algoritmo de
 * - ordenação simples, como bolha) e exiba a tabela final.
 *
 * ENTRADA:
 * Quantidade de partidas e, para cada uma, os índices dos dois times e o placar de gols de cada um.
 *
 * SAIDA ESPERADA:
 * A tabela final de classificação com time, vitórias, empates, derrotas e pontos, ordenada por pontos.
 *
 * EXEMPLOS:
 * Exemplo 1:
 * Entrada:
 *   1
 *   0
 *   1
 *   2
 *   1
 * Saida:
 *   Classificacao Final:
 *   1o Time A - V:1 E:0 D:0 Pts:3
 *   2o Time B - V:0 E:0 D:1 Pts:0
 *   3o Time C - V:0 E:0 D:0 Pts:0
 *   4o Time D - V:0 E:0 D:0 Pts:0
 *
 * CASOS DE TESTE PARA VALIDAR SUA SOLUCAO:
 * Caso 1: Partida que termina empatada (mesmo placar para os dois times).
 * Caso 2: Dois times terminam com a mesma pontuação, testando o critério de desempate
 * (mantenha a ordem original em caso de empate).
 * Caso 3: Torneio sem nenhuma partida informada (quantidade = 0).
 *
 * OBSERVACOES IMPORTANTES:
 * Os nomes dos 4 times (Time A, Time B, Time C, Time D) podem ser fixos no código.
 *
 * ==========================================================
 */

public class Main {

    public static void main(String[] args) {

        // Escreva sua solucao aqui

    }

}
